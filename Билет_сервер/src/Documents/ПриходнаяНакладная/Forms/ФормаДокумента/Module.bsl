
&НаКлиенте
Процедура ПояснениеОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка=Ложь;
	СтрокаПоиска=Элемент.ТекстРедактирования;
	Позиция=Найти(СтрокаПоиска, "e1cib");
	Если Позиция>0 Тогда
		НавигационнаяСсылка=Сред(СтрокаПоиска, Позиция);
		ПерейтиПоНавигационнойСсылке(НавигационнаяСсылка);
	КонецЕсли;
	
КонецПроцедуры
